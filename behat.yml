default:
    autoload:
        - "%paths.base%"
    suites:
        default:
            filters:
                tags: "~@incomplete"
            contexts:
                - tests\acceptance\BOF\YearlyReportContext:
